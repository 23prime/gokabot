version: '2.1'

services:
  gokabot-line-dev:
    image: gokabot-line-dev
    container_name: gokabot-line-dev
    volumes:
      - /etc/localtime:/etc/localtime:ro
    environment:
      TZ: Asia/Tokyo
    build: .
    ports:
      - "8081:8080"
    command: [
      "bundle", "exec", "rackup", "app-localhost/config.ru",
      "-o", "0.0.0.0",
      "-p", "8080"
    ]
